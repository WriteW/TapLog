<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TapLog">
        <activity
            android:name=".Start"
            android:exported="true"
            android:theme="@style/Theme.TapLog">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Select"/>
        <activity android:name=".Tap"/>
        <activity android:name=".Log"/>
        <activity android:name=".IsOkay"/>
        <activity android:name=".score.Score"/>
        <activity android:name=".score_ai.ScoreAIActivity"/>
        <activity android:name=".daily_ai.DailyActivity"
            android:windowSoftInputMode="adjustResize"/>
    </application>

</manifest>